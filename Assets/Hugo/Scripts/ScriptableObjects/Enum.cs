public enum CardType
{
    attack,
    heal,
    shield,
    special
}

public enum MaskState
{
    angry,
    joy,
    sad,
    none
}

public enum CardEffect
{
    burnBonus,
    healBonus,
    freezeBonus,
    none
}

public enum EnemyAttack
{
    Attack,
    Heal,
    Shield,
    StateChange,
    Special
}